{
  "name": "multimodal-asset-library",
  "version": "0.2.0",
  "private": true,
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "dev": "APP_MODE=dev ./scripts/start.sh",
    "build": "pnpm --dir backend run build && pnpm --dir frontend run build",
    "start": "APP_MODE=prd ./scripts/start.sh",
    "stop": "./scripts/stop.sh",
    "lint": "pnpm --dir frontend run lint",
    "typecheck": "pnpm --dir backend run typecheck && pnpm --dir frontend run typecheck",
    "test": "pnpm --dir backend run test && pnpm --dir frontend run test && node scripts/test-log-pipe.mjs",
    "test:logs": "node scripts/test-log-pipe.mjs",
    "db:generate": "pnpm --dir backend run db:generate",
    "db:migrate": "pnpm --dir backend run db:migrate"
  }
}
