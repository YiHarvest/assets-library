**召回改造实施记录**

2026-09-09，分支 `refactor/refactor_recall`。基线提交 `3706b8b2f135d7a92cba3628419f7a42aeb6fccd`。

公开接口及现有业务选材逻辑保持原样。当前线上仍使用 v1；新模块已接入 worker、变更/删除入队及两个共享召回入口。双写默认关闭，读版本默认 v1，尚未部署切换。阶段验收以本文件的实际证据为准，不能将计划中的质量目标当作实测结果。

| 阶段 | 当前进度 | 实际证据 / 剩余项 |
| --- | --- | --- |
| P0 | 契约基线与 ES 能力已验证 | 现有 66 个相关用例通过；真实 ES 8.11.3 的 9 项能力探针通过。真实质量基线仍需随 P4 冻结 |
| P1 | 核心实现与存量兼容检查完成 | 1,572 条当前素材全量只读构建无失败；10,000 字符样本完整覆盖；服务端实际文件与进程参数已核实，权重指纹已固定 |
| P2 | 核心可靠写入链路通过集成验证 | 真实 MySQL→快照→ES→水位链路、事务回滚、并发修订、失败后重试、删除后旧任务不复活均通过；双写默认关闭 |
| P3 | 共享召回与两个业务适配已接入 | 素材级两路召回、RRF、证据和诊断、同物理索引固定、有限扩窗通过测试；两个适配入口在真实 ES+BGE+MySQL 上通过权限/白名单/排除/返回字段验证 |
| P4 | 六方案重放与两个入口的共享召回性能诊断完成，质量验收未完成 | 同一 1,602 条素材快照，六方案各重放 200 条查询，1,306 对待判读样本；两个真实 repository 入口完成两轮各 800 次测量；独立 test、assets-query 真实查询分布、画面标注及受控负载验收仍待完成 |
| P5 | 回填、核对与 CLI 基础演练通过 | 主键游标回填、保留删除标记、漏入队检查和重扫修复通过真实 MySQL/ES；CLI 及真实查询首批影子重放通过技术核对；生产追平、质量和性能门槛尚未通过，未切换 |

**已核实结果**

- ES 8.11.3：nested kNN 按顶层素材去重、范围与删除过滤、最近块 inner hit、nested BM25 max、整篇替换、旧版本与同版本冲突、非法新文档保留旧版、删除标记防复活全部通过。探针只使用随机独立索引和二维合成向量，结束删除该测试索引；这不代表真实质量或负载验收。
- 当前服务声明 `bge-m3`，vLLM 0.11.2，输入上限 8,192 tokens。6 组中英文/数字/Unicode/特殊 token/空文本的本地 tokenizer 与服务端 token IDs 完全一致；实际向量为 1,024 维，L2 范数约 1。
- tokenizer 固定下载修订及两份文件 SHA-256 位于 [配置](../config/recall/bge-m3-tokenizer.json)。这些只证明 tokenizer 身份，不能据此声称线上模型权重也是该修订。
- 已另行在实际 embedding 服务主机核对 vLLM 进程、模型目录文件及 SHA-256；文件修改时间早于进程启动且散列期间未变化。可复核清单位于 [部署身份](../config/recall/bge-m3-serving-identity.json)，其中 `pytorch_model.bin` 为 2,271,145,830 bytes。组合指纹含实际文件和服务参数；模型部署变化后必须重新核实，不能把本次观察永久当作服务身份。
- 14:14 的线上只读快照：1,572 条未删除素材，1,571 条已完成，1 条完成但只有描述，1 条失败且无文本。新文档构建成功 1,572 条，结构异常 0，构建失败 0；共 9,797 个去重块，最多 16 块/素材，最大 167 tokens/块，合并后仍保留额外 2,638 个来源引用。
- 10,000 字符合成合法人工描述：31 块，最大 256 tokens，逐字符完整覆盖。切分使用真实 tokenizer 计数并保留原 Unicode 子串；避免直接 decode token IDs 导致未知字符或原始拼写丢失。配置硬上限 512，最大重叠 32，超限明确失败。
- v2 写入以素材为完整文档，元数据变更复用原向量；模型指纹变化重新生成，失败保留旧文档，同版本重试检查指纹，乱序结果和删除竞态通过单元测试及真实 ES 验证。

**验证命令**

```sh
pnpm exec vitest run tests/unit/elasticsearch-search.test.ts tests/unit/compatibility-match.test.ts tests/unit/api-v1-routes.test.ts tests/unit/contracts.test.ts --silent
pnpm exec vitest run tests/unit/recall-document.test.ts tests/unit/recall-manifest.test.ts tests/unit/recall-writer.test.ts tests/unit/recall-embedding.test.ts --silent
node --env-file=.env --import tsx scripts/probe-recall-es.ts
node --env-file=.env --import tsx scripts/probe-recall-model.ts
node --env-file=.env --import tsx scripts/audit-recall-sources.ts
pnpm run typecheck
```

ES 集成测试显式使用 `TEST_RECALL_ES_URL/USERNAME/PASSWORD`，只创建和删除随机 `test_recall_v2_*` 索引。MySQL 集成测试继续使用既有专用测试库保护，不能指向 WebUI 使用的库。

本次创建本地 Colima profile `recall-test`（2 CPU / 3 GiB）与 `recall-mysql` 容器，MySQL 仅绑定本机回环地址，库 `recall_integration_test`，用于迁移和事务测试。Docker 默认 context 已恢复为原 `colima`，收尾后已停止专用测试虚拟机。远程账号创建独立测试库被 MySQL 权限拒绝，没有在生产库执行迁移。

真实集成测试另发现 Drizzle 0.44 同时启用两个事务开始选项时生成无效 SQL。追平读取改为 `REPEATABLE READ + READ ONLY`，由第一次一致性读取建立快照；真实 MySQL 复测通过。检查会在 ES 扫描前后各取一次来源清单，检测扫描期间的变化；不会通过更新状态来伪造追平。

15:21 前后的验证：新增召回单元测试 26 个通过（其中增加跨调用 embedding 并发许可、排队取消与失败后恢复）；新 MySQL 版本/回填/ES 链路 4 个通过；CLI 运维演练 1 个通过；类型检查和新增/受影响文件 ESLint 通过。ES/业务适配先前集成验证见上表。完整安全扫描仍被基线 `tests/unit/integration-database-safety.test.ts` 的带端口测试 URL 阻挡，已确认该文件未修改且基线提交也命中；未宣称全仓检查全部通过。

按用户要求，已直接同步本地 `.env` 的 9 个新增召回配置，并通过真实 `loadConfig()` 解析验证模型 revision、策略文件和 tokenizer 路径。当前保持 `SEARCH_RECALL_ENGINE=v1`、`SEARCH_V2_WRITE_ENABLED=false`；其他配置赋值保持原样。`.env` 仍由 Git 忽略，后续配置切换由实施过程同步，用户无需手工同步。

15:27 左右的 P4 来源核对：近 14 天保留 83 个 match 作业、2,597 条分段文本，去掉纯文本重复后 1,986 种表达。只读一致性快照导出当时 1,602 条当前素材（506 个来源组），按文本+当前候选范围去重得到 2,322 条待评测查询，保存至被 Git 忽略的 `data/recall-evaluation-input-20260909.json`，权限 0600。该文件是未标注的准备输入，尚未划分主题、未重建历史逐段排除集合，也不覆盖 assets-query 入口，不能当作冻结测试集或效果报告。工具为 `scripts/export-recall-evaluation-input.ts`。

数据库回归发现三处既有失败，已在独立 worktree 的基线提交、另一全新 MySQL 测试库上逐项复现：`mysql-database.test.ts` 的旧列移除断言（406 行）、私人素材默认列表断言（972 行）、公共删除重试创建任务（1585 行）。本次不通过改动上层业务行为消除这些基线失败。新增表的清理清单排序已修正；新召回 DB 测试不与其他进程共用同一库并发执行。

16:06 前后的收尾验证：新增召回单元测试 **29 个全部通过**，类型检查通过。真实 ES/BGE 的评测 CLI 集成用例已验证六方案及冻结索引保护；最终真实 v1 重放也通过新增的实现指纹核对。此次只补充了必要的性能证据校验：`measurement.boundary` 缺失或为 `pinned-engine` 时，评测输出 `shared_recall_latency_missing`，阻止仅凭离线引擎耗时通过切换验收。

三个独立实验索引已完成并设置写保护，使用相同来源散列 `f1e2685c567ffaba1c63f77cdb5af077c2383a5114a0dec0a3985c3a1ad330ac`。未绑定业务读别名，也未向生产 MySQL 写入构建状态。

| 实验构建 | 素材 / 文本块 | 主分片体积（构建时） | 记录文件（本地受限） |
| --- | --- | --- | --- |
| visual-v2 | 1,602 / 9,969 | 208,627,471 bytes | `data/recall-eval-visual-build-20260909.json` |
| legacy-v1 分块消融 | 1,602 / 9,969 | 201,823,394 bytes | `data/recall-eval-legacy-build-20260909.json` |
| 重建的 v1 行为基线 | 1,602 / 9,969 | 202,443,214 bytes | `data/recall-eval-v1-build-20260909.json` |

新布局真实 embedding/写入约 611 秒；旧分块对照复用 9,967 个同模型、同输入的已核实向量，仅重算 2 个输入。不能将复用后的耗时作为分块性能优势。该批素材新旧分块数量相同，最长 167 tokens；长描述完整切分的行为由另外的长文本用例验证。

待判读草稿从 77 个任务来源轮流抽取 200 条查询，全部放在 tune，test 为 0、标签为 0。六种方案均重放其中前 20 条，共 120 次，错误、范围越界、重复素材均为 0。返回候选总数分别为 v1 6、纯语义 2、纯词法 162、混合 164、无元数据 162、旧分块混合 164；这些是候选数量，不是命中正确率。首批仅涉及 segment-match，不能代表两个入口的整体效果。

- `data/recall-judging-pool-20260909.json`：185 对查询与素材，待画面判读；每条查询带实际已重放方案列表，其余 180 条没有伪装成已覆盖。
- `data/recall-evaluation-readiness-20260909.json`：工具按预期退出 2，`eligibleForCutover=false`。独立测试、负例、专项样本、共享召回性能等证据缺失，未计算出可用于发布的质量结论。
- `data/recall-shadow-<system>-20260909.json`：六份受限运行记录。固定引擎 p95 仅用于首批诊断，不包括 MySQL 范围筛选、每请求别名解析与二次校验，不作为正式延迟验收。

质量验收需要真实画面判读和 assets-query 的代表性查询样本；样本可以直接提供，不要求部署位置或生产访问权限。共享召回链路的独立性能诊断已继续完成，结果见下文。期间保持 `.env` 中读 v1、双写关闭；环境同步由实施过程负责，用户无需手工操作。

16:21 左右补齐了全部 200 条草稿的六方案重放，共 1,200 次召回调用，错误、范围越界和重复素材均为 0。49 条查询的允许范围为空，六方案都直接返回空，不应将这 294 次空范围调用说成实际 ES/embedding 请求。正式负例质量统计现已排除空范围；新增回归先失败后通过，评测文件 6 个用例、类型检查和相关 ESLint 均通过。生产召回行为未改动，此次只修改评测计算的两行逻辑。

| 方案 | 有候选的查询数 / 200 | 查询与候选对数 | 固定引擎 p95（ms） |
| --- | --- | --- | --- |
| v1 | 5 | 14 | 328.83 |
| v2 纯语义 | 3 | 9 | 281.32 |
| v2 纯词法 | 63 | 1,249 | 255.79 |
| v2 混合 | 64 | 1,258 | 417.72 |
| v2 无元数据 | 63 | 1,205 | 621.71 |
| v2 旧分块混合 | 64 | 1,258 | 496.48 |

当前样本中新旧分块混合的候选排序完全相同；不能声称已有分块质量增益。候选扩展主要来自词法通道，是否相关仍未知。混合方案固定引擎 p95 约为 v1 的 1.27 倍，已高于计划 1.2 倍目标；这些顺序实验共用现有服务且缺少完整共享召回开销，既不能作为正式性能达标证明，也不能据此直接降低候选窗口。后续需要真实相关性标注及受控负载证据来判断取舍。

完整候选池为 `data/recall-judging-pool-full-20260909.json`，200 条均带六方案覆盖记录，共 1,306 对待判读样本；完整计数与边界见 `data/recall-full-replay-observations-20260909.json`。`data/recall-evaluation-readiness-full-20260909.json` 仍为 `eligibleForCutover=false`。这些文件继续使用 0600 权限并由 Git 忽略。

容量记录 `data/recall-capacity-observations-20260909.json` 给出每素材块数分布：中位数 7、p95 为 9、最大 16（4 条素材）；新布局初次构建的单素材写入 p95 约 1,018 ms，使用延后 refresh，不等同线上更新可见延迟。

另在随机独立索引测量这 4 条最大文档的真实更新，使用生产 writer 的 `refresh=wait_for`，每次写后检查搜索可见内容与 revision：名称更新共 0 次新向量输入，中位 1,001 ms；同版本重试 0 次，中位 70 ms；描述更新每条仅 1 次新向量输入，中位 712 ms。刷新周期位置会影响单次耗时，4 条样本不是负载 SLA，不能据此推断描述更新比名称更新更快。实验索引已删除并以 HEAD 404 核实；记录为 `data/recall-update-cost-20260909.json`。未修改生产素材、MySQL 或业务读别名。

17:05 前后的性能诊断与最小修复：

- 先用 151 条非空范围查询交替执行 v1/v2 固定引擎测量，同一冻结索引、单并发，v1 p95 为 643.51 ms，v2 为 684.79 ms，比例 1.064。与先前顺序重放的 1.27 不同，说明现有 ES/模型服务的背景负载波动明显；不能只挑较好的一轮作为达标证据。记录：`data/recall-paired-latency-20260909.json`。
- 再在本地专用 MySQL 装载实际快照的 1,602 条素材及标签，调用两个真实 repository 入口；从 MySQL 范围筛选之前开始计时，到共享召回和二次校验完成截止。每入口 200 条查询，v1/v2 交替执行，每轮 800 个案例。私人素材使用合成的测试所有者；assets-query 借用 segment-match 查询文本，因此这里只证明调用链和该工作负载，不能代表其真实查询分布。
- 合并 v2 读别名和 mapping 查询：通过一次 `GET /<read-alias>?features=aliases,mappings` 同时验证唯一索引、环境、无过滤/路由及原有清单完整性。两个生产文件净增 4 行，没有新增缓存或配置。预热后的非空查询，索引解析请求数由每次 2 个降至 1 个；首次加载 runtime 仍有原有的构建核对。

| 共享召回入口 | 合并前 v1 / v2 p95（ms） | 合并后 v1 / v2 p95（ms） | 合并后 v2/v1 |
| --- | --- | --- | --- |
| assets-query | 293.80 / 523.71 | 488.31 / 577.88 | 1.183 |
| segment-match | 345.09 / 592.81 | 1,089.45 / 1,379.21 | 1.266 |

上述每组均为 200 个案例、最近秩 p95，包含按业务范围直接返回空的情况。两个版本的绝对耗时均有明显波动；只确认请求数量减少，未证明绝对延迟改善，也未通过 1.2 倍性能门槛。两轮 v2 分别从同一冻结文档/向量复制到随机独立索引，构建 ID 与清单散列不同。报告为 `data/recall-scoped-latency-{baseline,combined,comparison}-20260909.json`；前两份各自记录 `complete=true`、`cleanedUp=true`，没有修改业务读别名。

逐一比较两轮的 800 个案例，候选集合全部相同，1 个 v2 案例排序不同。核对发现两个素材的向量原始分数同为 0.7231728，ES 内部顺序会改变 RRF 的排名贡献。新增真实 ES 测试先复现失败，再仅增加一行向量查询排序（`_score desc, assetId asc`），与词法通道保持一致；3 个真实 ES 集成测试全部通过。相同真实查询、固定向量和原冻结索引连续测量 3 次，17 个候选集合保持一致，同分顺序稳定。该规则稳定返回候选中的同分排序，不保证 ANN 截断处的候选成员在所有重建或模型运行中恒定。诊断和修复记录分别为 `data/recall-ranking-difference-20260909.json`、`data/recall-ranking-tie-fix-20260909.json`；上述两轮性能数据采集于此排序修复之前。

最终新增召回单元测试 **31 个通过**、真实 ES 集成测试 **3 个通过**，类型检查及本轮受影响文件 ESLint 通过。现有 API 入参、出参结构与上层选材流程没有变化。实际 `.env` 已再次验证：读 v1、双写关闭，策略和 tokenizer 路径可用；本轮优化不需要新增环境变量。临时测量程序归档在被 Git 忽略的 `data/` 下，相关报告保持 0600 权限。正式质量与受控负载验收尚未完成，P4/P5 不能标记为发布通过。

参考：[Elastic 8.11 nested kNN](https://www.elastic.co/guide/en/elasticsearch/reference/8.11/knn-search.html#nested-knn-search)、[Get index 的 features 参数](https://www.elastic.co/guide/en/elasticsearch/reference/8.11/indices-get-index.html)、[Hugging Face Tokenizers.js](https://github.com/huggingface/tokenizers.js)、[固定 BGE tokenizer 配置](https://huggingface.co/BAAI/bge-m3/blob/5617a9f61b028005a4858fdac845db406aefb181/tokenizer_config.json)。

17:59：业务真实请求 RC202609081703000001 的上下文修复与回放。

- 用户明确要求最小改动，接口入参、出参、默认值不变。本轮只增加局部上下文构造和内部传递，在 v2 中单独嵌入当前片段/上下文，再以 0.7/0.3 混合归一化。未添加环境变量、依赖、重排模型或公开字段；v1 保留原有回退行为。混合比例仍是实验值。
- 上下文来自现有 `llm.segments` 的连续分组，`group_id` 按组内位置和长度验证，不能按重复元组全局聚合。保留当前句与前一句，最多 1024 个 Unicode 字符；原分段、keyword、level、group_id、时间轴和随机/顺序选材规则不改。
- BM25 独有候选补做相同阈值的向量校验，通过才参与 RRF；仍能补入 ANN 初始窗口之外的合格候选。新增回归先复现上下文丢失和词法绕过语义门槛，再通过修复；真实 ES 验证了语义相反的同名素材被排除，以及上下文对排序的实际影响。
- 11 个指定素材实时只读核对均可用，描述与冻结快照一致；下载原视频并检查 33 张抽帧。使用真实 matcher、repository、v1/v2 搜索函数及现有模型/ES 回放 36 段，MySQL 固定在 READ ONLY 事务。v2 的测试读绑定仅在回放进程内指向冻结索引，没有改变实际别名、索引或线上数据。未发送任务或回调。
- 最后一轮：v1 顺序匹配 1 段；v2 顺序和默认随机均匹配 3 段、未匹配 33 段。所有运行的越界、重复素材、分段字段改动均为 0。默认随机结果中第 26 段匹配通讯录未读消息，第 34 段匹配评论区；第 24 段匹配商场柜台，画面适配性存疑。顺序模式第 24 段匹配服装店橱窗，不能准确表达店员忙碌。
- **质量不通过。** 第 13 段刷手机、第 16 段放置手机存在明确可用素材但未命中；第 32 段仍未可靠理解“它”指手机。当前模型表示下，错误的橱窗候选相似度约 0.6256，高于刷手机正确候选的约 0.6230，说明简单降低统一阈值不能解决这些区分。没有为提高命中数量降低正式阈值，也没有把无候选全部判成错误或正确。
- 相关单元测试 53 个、真实 ES 集成测试 4 个通过；类型检查、相关 ESLint 通过。108 条回放分段逐一验证输出字段集合与原有格式一致，上下文与评语均未进入业务 JSON。
- 受保护产物目录：`data/recall-business-case-20260909/`。`fixed-default.json` 为默认随机选材的完整业务结果；`fixed-ordered.json` 为顺序结果；`baseline-ordered.json` 为 v1 对照；`review.json` 是独立的逐段评审，明确 `overall_passed=false`、`human_confirmed=false`。`execution.json` 记录调用边界和执行参数；`replay.cjs` 可重放。未过阈值的诊断候选放在单独文件，不能当作已命中结果。
- `.env` 与 `.env.example` 已同步本轮状态说明；`SEARCH_RECALL_ENGINE=v1`、双写关闭保持。P4/P5 质量门槛仍未完成，不能将本次修复当作已可切换 v2 的结论。素材侧向量策略本轮未变，存量资产无需重导入。

18:07：用户进一步明确素材时间轴要对应文本时间轴。

- 验收调整为相关配画面与时长覆盖，不要求画面逐字呈现或证明文案中的宣传事实。早先关于“证明获客数量”等评语不再作为必要条件；独立 review.json 已修正。缺失素材和漏召回仍需区分。
- 最小实现只再增加内部 `minDurationMs`：匹配器从原有 `end_time-start_time` 换算毫秒，repository 在召回前过滤短于目标片段的视频。图片不受此限制；没有时长要求的普通素材查询保持原行为。视频时间信息未知时无法保证覆盖，带时长条件的查询会排除它。现有快照的 1,522 个视频均有分镜起止时间。公开请求/响应未添加字段，原时间轴也未修改。
- 真实只读 MySQL + ES 回归先证明 800 ms 素材会错误通过 840 ms 条件，修复后验证：无条件可命中、800 ms 条件可命中、840 ms 条件被排除，3 种边界均通过。21 个受影响单元测试、类型检查、相关 ESLint 通过。
- 更新后的默认随机回放仍为 3 段命中 / 33 段未命中，越界、重复素材和时间字段改动均为 0。第 24/26/34 段分别需要 0.84/1.12/1.40 秒，实际素材为 9.00/2.434/3.167 秒，均足够覆盖。第 24 段本次随机选中 C03，属于店铺背景，具体忙碌动作表达较弱。C02 店员画面只有 0.80 秒，不能覆盖第 24 段；此前把它作为更好候选的评审没有考虑时长，现已纠正。
- `timeline-check.json` 是独立时间轴核对文件；`fixed-default.json` 保持原业务结果结构。这只能说明已命中的 3 段时长足够，不能当作 36 段全部已有素材。最终查询阈值与 v2 质量验收仍未完成，线上没有切换。
